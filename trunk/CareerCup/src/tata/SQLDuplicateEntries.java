package tata;

/*
 * select name, count(name) from student group by name having count(name)>1
 * SELECT * FROM TABLE MINUS SELECT DISTINCT FROM TABLE
 */
public class SQLDuplicateEntries {

}
