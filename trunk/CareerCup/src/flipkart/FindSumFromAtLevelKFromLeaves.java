package flipkart;

/*
 * 1) Create a map
 * 2) Do post order traversal
 * 	-For each leaf add its level to the map - map.put(leaf.level, 0);
 * 	-For each node which is not leaf check if map.contains(node.level + k)
 * 		- if yes add node.value
 */
public class FindSumFromAtLevelKFromLeaves {

}
