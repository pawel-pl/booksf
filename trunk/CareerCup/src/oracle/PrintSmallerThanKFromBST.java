package oracle;

/*
 * void printSmaller (Tree *root, int k) {
    if (!root) return;
        
    printSmaller(root->left, k);
    if (root->data < k) {
        printf("%d ", root->data);
        printSmaller(root->right, k);
    }
}
 */
public class PrintSmallerThanKFromBST {


}
