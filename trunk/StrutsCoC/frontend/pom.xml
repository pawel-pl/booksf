<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
	xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/maven-v4_0_0.xsd">
	<parent>
		<artifactId>StrutsCoC</artifactId>
		<groupId>StrutsCoC</groupId>
		<version>0.0.1-SNAPSHOT</version>
	</parent>
	<modelVersion>4.0.0</modelVersion>
	<groupId>StrutsCoC</groupId>
	<artifactId>StrutsCoC-frontend</artifactId>
	<packaging>war</packaging>
	<name>${project.artifactId}</name>

	<dependencies>
		<dependency>
			<groupId>StrutsCoC</groupId>
			<artifactId>StrutsCoC-core</artifactId>
			<version>${project.version}</version>
		</dependency>
		<dependency>
			<groupId>StrutsCoC</groupId>
			<artifactId>StrutsCoC-model</artifactId>
			<version>${project.version}</version>
		</dependency>
	</dependencies>
	<build>
		<finalName>BookshelfStruts</finalName>
		<plugins>
			<plugin>
				<artifactId>maven-resources-plugin</artifactId>
				<executions>
					<execution>
						<id>copy-resources</id>
						<phase>validate</phase>
						<goals>
							<goal>copy-resources</goal>
						</goals>
						<configuration>
							<outputDirectory>${project.build.directory}/BookshelfStruts</outputDirectory>
							<resources>
								<resource>
									<directory>src/main/resources</directory>
									<filtering>false</filtering>
									<excludes>
										<exclude>validation/*</exclude>
										<exclude>BookshelfSupport_en.properties</exclude>
										<exclude>BookshelfSupport_pl.properties</exclude>
										<exclude>xwork-conversion.properties</exclude>
										<exclude>struts.properties</exclude>
									</excludes>
								</resource>
								<!-- <resource>
									<directory>src/main/resources/validation</directory>
									<filtering>false</filtering>
									<includes>
										<include>*.*</include>
									</includes>
									<targetPath>../classes</targetPath>
								</resource> -->
								<resource>
									<directory>src/main/resources/</directory>
									<filtering>false</filtering>
									<includes>
										<include>BookshelfSupport_en.properties</include>
										<include>BookshelfSupport_pl.properties</include>
									</includes>
									<targetPath>../classes/bookshelf/frontend/action</targetPath>
								</resource>
							</resources>
						</configuration>
					</execution>
				</executions>
			</plugin>
		</plugins>
		<resources>
			<resource>
				<directory>src/main/resources</directory>
				<excludes>
					<exclude>css/**</exclude>
					<exclude>images/**</exclude>
					<exclude>images/**</exclude>
					<exclude>BookshelfSupport_pl.properties</exclude>
					<exclude>BookshelfSupport_en.properties</exclude>
				</excludes>
			</resource>
		</resources>
	</build>

</project>