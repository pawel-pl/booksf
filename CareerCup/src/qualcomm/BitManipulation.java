package qualcomm;

/*
 * b = (byte) (b | (1 << 6));
 * To set the sixth bit to zero:
 * b = (byte) (b & ~(1 << 5));
 * 
 * AND with 111000001111 (clean - set to 0) and then OR the result with 000xxxxx0000 
 */
public class BitManipulation {

	/**
	 * @param args
	 */
	public static void main(String[] args) {
		
		

	}

}
